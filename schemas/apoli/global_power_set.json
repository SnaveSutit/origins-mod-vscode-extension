{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"$docsUrl": "https://origins.readthedocs.io/en/latest/json/global_power_set/",
	"type": "object",
	"properties": {
		"entity_types": {
			"type": "array",
			"items": {
				"$ref": "types/identifier.json"
			},
			"description": "If specified, the specified powers will only be granted to entities that fulfill this tag-like specifier.",
			"markdownDescription": "If specified, the specified powers will only be granted to entities that fulfill this tag-like specifier."
		},
		"powers": {
			"type": "array",
			"items": {
				"$ref": "power.json"
			},
			"description": "The ID(s) of the power(s) to grant to entities globally.",
			"markdownDescription": "The ID(s) of the power(s) to grant to entities globally."
		},
		"order": {
			"type": "integer",
			"default": 0,
			"description": "Determines the order at which the global power set is applied.",
			"markdownDescription": "Determines the order at which the global power set is applied."
		},
		"type": {
			"description": "\n\nThis is the format of a JSON file describing a global power set. Global power sets are sets of powers that are granted to entities globally with the `apoli:global` power source. It can also be filtered so that the set of powers will only be granted to certain entities of certain type or entities included in a certain entity type tag.\n\nGlobal Power Set JSON files need to be placed inside the `data/<namespace>/global_powers` folder of a datapack.\n\n\n",
			"markdownDescription": "\n\nThis is the format of a JSON file describing a global power set. Global power sets are sets of powers that are granted to entities globally with the `apoli:global` power source. It can also be filtered so that the set of powers will only be granted to certain entities of certain type or entities included in a certain entity type tag.\n\nGlobal Power Set JSON files need to be placed inside the `data/<namespace>/global_powers` folder of a datapack.\n\n\n"
		}
	}
}