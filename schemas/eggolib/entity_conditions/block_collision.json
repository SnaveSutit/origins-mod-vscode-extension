{
	"$id": "https://snavesutit.github.io/origins-mod-schemas/schemas/eggolib/entity_conditions/block_collision.json",
	"type": "object",
	"required": [
		"type"
	],
	"properties": {
		"type": {
			"description": "eggolib:block_collision\nChecks whether the bounding box of the entity is colliding with a block.\nEntity Condition Types operate on an Entity, which also allows access to the world. These are available to be used in most powers in the condition object field (or entity_condition in other power/condition types), which restricts when a power is active.",
			"markdownDescription": "### `eggolib:block_collision`\nChecks whether the bounding box of the entity is colliding with a block.\n## Entity Condition Type\nEntity Condition Types operate on an Entity, which also allows access to the world. These are available to be used in most powers in the condition object field (or entity_condition in other power/condition types), which restricts when a power is active.\n\n[Entity Condition Types Documentation](https://origins.readthedocs.io/en/latest/types/entity_Condition_types/)",
			"const": "eggolib:block_collision"
		},
		"block_condition": {
			"description": "If specified, the condition will only evaluate to true if this condition is fulfilled by the block colliding with the bounding box of the entity.",
			"$ref": "../../block_condition.json"
		},
		"offset_x": {
			"description": "Determines the offset for the bounding box of the entity in the X axis.",
			"type": "number",
			"default": 0
		},
		"offset_y": {
			"description": "Determines the offset for the bounding box of the entity in the Y axis.",
			"type": "number",
			"default": 0
		},
		"offset_z": {
			"description": "Determines the offset for the bounding box of the entity in the Z axis.",
			"type": "number",
			"default": 0
		}
	}
}