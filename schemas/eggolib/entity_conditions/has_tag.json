{
	"$id": "https://snavesutit.github.io/origins-mod-schemas/schemas/eggolib/entity_conditions/has_tag.json",
	"type": "object",
	"required": [
		"type"
	],
	"properties": {
		"type": {
			"description": "eggolib:has_tag\nChecks if the entity has a scoreboard tag (added via the /tag command).\nEntity Condition Types operate on an Entity, which also allows access to the world. These are available to be used in most powers in the condition object field (or entity_condition in other power/condition types), which restricts when a power is active.",
			"markdownDescription": "### `eggolib:has_tag`\nChecks if the entity has a scoreboard tag (added via the /tag command).\n## Entity Condition Type\nEntity Condition Types operate on an Entity, which also allows access to the world. These are available to be used in most powers in the condition object field (or entity_condition in other power/condition types), which restricts when a power is active.\n\n[Entity Condition Types Documentation](https://origins.readthedocs.io/en/latest/types/entity_Condition_types/)",
			"const": "eggolib:has_tag"
		},
		"tag": {
			"description": "String\toptional\tIf specified, checks if the entity has this scoreboard tag.",
			"type": "string"
		},
		"tags": {
			"description": "Array of Strings\toptional\tIf specified, checks if the entity has these scoreboard tags.",
			"type": "array",
			"items": {
				"type": "string"
			}
		}
	}
}