$id: https://snavesutit.github.io/origins-mod-schemas/
type: object
required:
  - type
properties:
  type:
    description: |-
      $namespace:$fileName
      Opens an inventory.
      $parentDescription
    markdownDescription: |-
      ### `$namespace:$fileName`
      Opens an inventory.
      $parentDescription
    const: $namespace:$fileName

  power:
    description: If specified, the inventory of this power will be opened instead.
    $ref: ../../types/identifier.json
