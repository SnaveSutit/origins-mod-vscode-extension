$id: https://snavesutit.github.io/origins-mod-schemas/
type: object
required:
  - type
properties:
  type:
    description: |-
      $namespace:$fileName
      Mounts the actor entity onto the target entity.
      $parentDescription
    markdownDescription: |-
      ### `$namespace:$fileName`
      Mounts the actor entity onto the target entity.
      $parentDescription
    enum:
      - $namespace:$fileName
      - origins:$fileName
