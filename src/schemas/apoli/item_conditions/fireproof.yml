$id: https://snavesutit.github.io/origins-mod-schemas/
type: object
required:
  - type
properties:
  type:
    description: |-
      $namespace:$fileName
      Checks if the item stack is fireproof.
      $parentDescription
    markdownDescription: |-
      ### `$namespace:$fileName`
      Checks if the item stack is fireproof.
      $parentDescription
    enum:
      - $namespace:$fileName
      - origins:$fileName
