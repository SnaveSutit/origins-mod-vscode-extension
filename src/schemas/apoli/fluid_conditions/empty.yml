$id: https://snavesutit.github.io/origins-mod-schemas/
type: object
required:
  - type
properties:
  type:
    description: |-
      $namespace:$fileName
      Checks whether there is no fluid in this state.
      $parentDescription
    markdownDescription: |-
      ### `$namespace:$fileName`
      Checks whether there is no fluid in this state.
      $parentDescription
    enum:
      - $namespace:$fileName
      - origins:$fileName
