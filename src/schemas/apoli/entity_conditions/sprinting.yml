$id: https://snavesutit.github.io/origins-mod-schemas/
type: object
required:
  - type
properties:
  type:
    description: |-
      $namespace:$fileName
      Checks whether the entity is currently sprinting.
      $parentDescription
    markdownDescription: |-
      ### `$namespace:$fileName`
      Checks whether the entity is currently sprinting.
      $parentDescription
    enum:
      - $namespace:$fileName
      - origins:$fileName
