$id: https://snavesutit.github.io/origins-mod-schemas/
type: object
required:
  - type
properties:
  type:
    description: |-
      $namespace:$fileName
      Checks if the entity exists.
      $parentDescription
    markdownDescription: |-
      ### `$namespace:$fileName`
      Checks if the entity exists.
      $parentDescription
    enum:
      - $namespace:$fileName
      - origins:$fileName
