$DONT_EXPORT: true #!! Remove this after copying the file!
$id: https://snavesutit.github.io/origins-mod-schemas/
type: object
properties:
  type:
    description: |-
      $namespace:$fileName
      TYPE TEMPLATE FILE
      $parentDescription
    markdownDescription: |-
      ### `$namespace:$fileName`
      TYPE TEMPLATE FILE
      $parentDescription
    $ignoreInvalidRef: true #!! Remove this after copying the file!
    $ref: ../../types/identifier.json
