$id: https://snavesutit.github.io/origins-mod-schemas/
type: object
required:
  - type
properties:
  type:
    description: |-
      $namespace:$fileName
      Dismounts the entity from their vehicle.
      $parentDescription
    markdownDescription: |-
      ### `$namespace:$fileName`
      Dismounts the entity from their vehicle.
      $parentDescription
    enum:
      - $namespace:$fileName
      - origins:$fileName
