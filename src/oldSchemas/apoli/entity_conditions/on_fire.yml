$id: https://snavesutit.github.io/origins-mod-schemas/
type: object
required:
  - type
properties:
  type:
    description: |-
      $namespace:$fileName
      Checks if the entity is currently burning.
      $parentDescription
    markdownDescription: |-
      ### `$namespace:$fileName`
      Checks if the entity is currently burning.
      $parentDescription
    enum:
      - $namespace:$fileName
      - origins:$fileName
