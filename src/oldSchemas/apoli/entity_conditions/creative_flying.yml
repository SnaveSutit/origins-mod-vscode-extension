$id: https://snavesutit.github.io/origins-mod-schemas/
type: object
required:
  - type
properties:
  type:
    description: |-
      $namespace:$fileName
      Checks if the player is Creative Mode flying.
      $parentDescription
    markdownDescription: |-
      ### `$namespace:$fileName`
      Checks if the player is Creative Mode flying.
      $parentDescription
    enum:
      - $namespace:$fileName
      - origins:$fileName
