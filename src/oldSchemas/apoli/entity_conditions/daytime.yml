$id: https://snavesutit.github.io/origins-mod-schemas/
type: object
required:
  - type
properties:
  type:
    description: |-
      $namespace:$fileName
      Checks whether it is daytime in the world.
      $parentDescription
    markdownDescription: |-
      ### `$namespace:$fileName`
      Checks whether it is daytime in the world.
      $parentDescription
    enum:
      - $namespace:$fileName
      - origins:$fileName
