$id: https://snavesutit.github.io/origins-mod-schemas/
type: object
required:
  - type
properties:
  type:
    description: |-
      $namespace:$fileName
      Checks if it's raining at the entity's position.
      $parentDescription
    markdownDescription: |-
      ### `$namespace:$fileName`
      Checks if it's raining at the entity's position.
      $parentDescription
    enum:
      - $namespace:$fileName
      - origins:$fileName
