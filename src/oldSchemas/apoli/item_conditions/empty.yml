$id: https://snavesutit.github.io/origins-mod-schemas/
type: object
required:
  - type
properties:
  type:
    description: |-
      $namespace:$fileName
      Checks if there is no item.
      $parentDescription
    markdownDescription: |-
      ### `$namespace:$fileName`
      Checks if there is no item.
      $parentDescription
    enum:
      - $namespace:$fileName
      - origins:$fileName
