$id: https://snavesutit.github.io/origins-mod-schemas/
type: object
required:
  - type
properties:
  type:
    description: |-
      $namespace:$fileName
      Checks whether the item is a food component.
      $parentDescription
    markdownDescription: |-
      ### `$namespace:$fileName`
      Checks whether the item is a food component.
      $parentDescription
    enum:
      - $namespace:$fileName
      - origins:$fileName
