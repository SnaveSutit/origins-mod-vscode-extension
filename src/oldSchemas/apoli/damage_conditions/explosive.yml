$id: https://snavesutit.github.io/origins-mod-schemas/
type: object
required:
  - type
properties:
  type:
    description: |-
      $namespace:$fileName
      Checks whether the damage source is an explosive.
      $parentDescription
    markdownDescription: |-
      ### `$namespace:$fileName`
      Checks whether the damage source is an explosive.
      $parentDescription
    enum:
      - $namespace:$fileName
      - origins:$fileName
