$id: https://snavesutit.github.io/origins-mod-schemas/
type: object
required:
  - type
properties:
  type:
    description: |-
      $namespace:$fileName
      This power type can be used by addon developers by creating a new PowerTypeReference to provide functionality.
      $parentDescription
    markdownDescription: |-
      ### `$namespace:$fileName`
      This power type can be used by addon developers by creating a new PowerTypeReference to provide functionality.
      $parentDescription
    enum:
      - $namespace:$fileName
      - origins:$fileName
