$id: https://snavesutit.github.io/origins-mod-schemas/
type: object
required:
  - type
properties:
  type:
    description: |-
      $namespace:$fileName
      Allows the player to swim (outside of water!).
      $parentDescription
    markdownDescription: |-
      ### `$namespace:$fileName`
      Allows the player to swim (outside of water!).
      $parentDescription
    enum:
      - $namespace:$fileName
      - origins:$fileName
